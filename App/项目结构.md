/src
  /OmniMind.Shared
    OmniMind.Enums
    OmniMind.Contracts        // DTO、请求响应、SignalR消息协议
    OmniMind.Abstractions     // 接口、通用抽象（IClock、ICurrentUser、IStorage、IQueue）
    OmniMind.Kernel           // 通用工具、Result、分页、异常、扩展

  /OmniMind.Domain
    OmniMind.Entities         // EF实体（或纯领域实体）
    OmniMind.Domain           // 领域服务、聚合根、值对象、领域事件、规则

  /OmniMind.Infrastructure
    OmniMind.Persistence.MySql // EFCore DbContext、Migrations、Repo实现
    OmniMind.Storage.Minio     // MinIO 实现（IObjectStorage）
    OmniMind.Vector.Qdrant     // Qdrant 实现（IVectorStore）
    OmniMind.Messaging.RabbitMQ// RabbitMQ 实现（IMessageBus）
    OmniMind.Realtime.SignalR  // SignalR Hub / 推送实现

  /OmniMind.Application
    OmniMind.Application       // UseCase / CQRS / 服务编排（不依赖EF，不依赖Minio）
    OmniMind.Orchestrator      // AI编排核心（多模态检索+工具调用+策略）
    OmniMind.Ingestion         // 摄入流程（解析→切片→向量化→索引）

  /OmniMind.Api
    OmniMind.Api               // Web API入口（Controllers、Auth、DI、Swagger）
